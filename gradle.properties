# Coordinates. JitPack overrides these via the GROUP / VERSION environment
# variables it injects into the build (see build.gradle.kts).
group=com.github.asuka1975
version=1.0.1

org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true
